# Copy this file to .env and fill in your values.
# The actual .env is gitignored — never commit real keys.

# Required: OpenAI — used for embeddings, LLM readings, and the RAG chat graph
OPENAI_API_KEY=sk-proj-...

# Optional: LangSmith — only needed if you want to use the Agent Response Evaluator
LANGCHAIN_API_KEY=lsv2_pt_...
